stages:
  - test
  - build

test:
  stage: test
  script:
    - ./ci/run-all.sh
  tags:
    - docker

build:
  stage: build
  script:
    - docker login -u $QUAY_USER -p $QUAY_PASSWORD quay.io
    - ./ci/build-image.sh
  tags:
    - docker
